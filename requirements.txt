Django==5.1.3
gunicorn==23.0.0
whitenoise==6.4.0
Pillow==10.4.0
sqlparse==0.5.3
requests==2.32.5
dj-database-url==1.0.0
python-dotenv==1.0.0
psycopg2-binary==2.9.6

# NOTE:
# The original requirements contained many machine-learning and local-only
# packages (dlib, face-recognition, tensorflow, opencv, etc.) that are
# unnecessary for deploying this Django web app to Render and cause build
# failures because they need heavy compilation toolchains. Keep ML/deep
# learning dependencies in a separate requirements-dev.txt if needed.
